name: belly_balance
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: '>=3.7.2 <4.0.0'  # Dart SDK version constraint

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.13.1  # Firebase Core SDK
  google_sign_in: ^6.2.1
  firebase_auth: ^5.5.4    # Firebase Authentication SDK
  cloud_firestore: ^5.5.0  # Firestore SDK
  firebase_storage: ^12.4.6  # Firebase Storage SDK
  intl: ^0.19.0
  cupertino_icons: ^1.0.8
  fl_chart: ^0.63.0
  http: ^1.4.0
  archive: ^3.1.2
  provider: ^6.1.2
  flutter_svg: ^2.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/images/B.png
    - assets/images/google_logo.png
    - assets/images/bb_logo.svg
  uses-material-design: true
